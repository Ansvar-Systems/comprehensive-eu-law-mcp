#!/usr/bin/env tsx
/**
 * Full ingestion pipeline — fetches upstream data, rebuilds DB, updates coverage.
 * Stub: run `npm run ingest:fetch && npm run build:db && npm run coverage:update` instead.
 */
console.log('Full ingestion not yet implemented. Use individual steps:');
console.log('  npm run ingest:fetch');
console.log('  npm run build:db');
console.log('  npm run coverage:update');
process.exit(0);
